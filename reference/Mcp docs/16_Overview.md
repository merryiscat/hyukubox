# MCP 기본 프로토콜 (Base Protocol) - 개요

## 프로토콜 개정판

**현재 버전**: 2025-11-25

---

## 프로토콜 구성 요소

Model Context Protocol은 함께 작동하는 여러 핵심 구성 요소로 구성됩니다:

### 필수 구성 요소

**Base Protocol (기본 프로토콜):**

- ✅ 핵심 JSON-RPC 메시지 타입
- ✅ 모든 구현에서 **필수(MUST)** 지원

**Lifecycle Management (수명 주기 관리):**

- ✅ 연결 초기화
- ✅ 기능 협상
- ✅ 세션 제어
- ✅ 모든 구현에서 **필수(MUST)** 지원

---

### 선택적 구성 요소

**Authorization (인증):**

- 📋 HTTP 기반 전송을 위한 인증 및 권한 부여 프레임워크
- 📋 애플리케이션 필요에 따라 **선택(MAY)** 구현

**Server Features (서버 기능):**

- 📋 서버가 노출하는 리소스, 프롬프트, 도구
- 📋 애플리케이션 필요에 따라 **선택(MAY)** 구현

**Client Features (클라이언트 기능):**

- 📋 클라이언트가 제공하는 샘플링 및 루트 디렉토리 목록
- 📋 애플리케이션 필요에 따라 **선택(MAY)** 구현

**Utilities (유틸리티):**

- 📋 로깅 및 인수 완성과 같은 범용 기능
- 📋 애플리케이션 필요에 따라 **선택(MAY)** 구현

---

### 설계 철학

**명확한 관심사 분리:**

- 각 계층은 특정 책임을 가짐
- 모듈식 설계로 필요한 기능만 지원 가능
- 클라이언트와 서버 간 풍부한 상호작용 가능

**유연한 구현:**

- 애플리케이션의 특정 요구사항에 따라 구성 요소 선택
- 필수 기능은 모두 지원하되, 선택적 기능은 필요시 추가

---

## 메시지 (Messages)

MCP 클라이언트와 서버 간의 모든 메시지는 **반드시(MUST)** [JSON-RPC 2.0](https://www.jsonrpc.org/specification) 사양을 따라야 합니다.

프로토콜은 다음 메시지 유형을 정의합니다:

```
┌─────────────────────────────────┐
│      JSON-RPC 2.0 메시지        │
├─────────────────────────────────┤
│  ┌──────────────────────────┐  │
│  │   Requests (요청)        │  │
│  │   - 작업 시작            │  │
│  │   - ID 필수              │  │
│  └──────────────────────────┘  │
│                                 │
│  ┌──────────────────────────┐  │
│  │   Responses (응답)       │  │
│  │   - Result (성공)        │  │
│  │   - Error (실패)         │  │
│  └──────────────────────────┘  │
│                                 │
│  ┌──────────────────────────┐  │
│  │   Notifications (알림)   │  │
│  │   - 단방향 메시지        │  │
│  │   - ID 없음              │  │
│  └──────────────────────────┘  │
└─────────────────────────────────┘
```

---

### 1. Requests (요청)

**정의:** [요청](https://claude.ai/specification/2025-11-25/schema#jsonrpcrequest)은 클라이언트에서 서버로 또는 그 반대로 작업을 시작하기 위해 전송됩니다.

**메시지 구조:**

```json
{
  "jsonrpc": "2.0",
  "id": "req-123",
  "method": "tools/list",
  "params": {
    "cursor": "next-page"
  }
}
```

**필드 설명:**

|필드|타입|필수|설명|
|---|---|---|---|
|`jsonrpc`|string|✅|항상 "2.0"|
|`id`|string \| number|✅|요청 식별자|
|`method`|string|✅|호출할 메서드 이름|
|`params`|object|❌|메서드 매개변수|

---

#### ID 요구사항

**필수 사항:**

- ✅ 요청은 **반드시(MUST)** 문자열 또는 정수 ID를 포함해야 함

**중요 규칙:**

- ❌ 기본 JSON-RPC와 달리 ID는 **절대(MUST NOT)** `null`이어서는 안 됨
- ❌ 요청 ID는 동일한 세션 내에서 요청자가 이전에 사용한 적이 **없어야(MUST NOT)** 함

**ID 예시:**

```json
// ✅ 올바른 ID
{ "id": "request-1" }
{ "id": 12345 }
{ "id": "uuid-abc-123" }

// ❌ 잘못된 ID
{ "id": null }  // null 불가
{ "id": "request-1" }  // 이미 사용된 경우
```

---

#### 메서드 예시

**도구 목록 가져오기:**

```json
{
  "jsonrpc": "2.0",
  "id": "1",
  "method": "tools/list",
  "params": {}
}
```

**리소스 읽기:**

```json
{
  "jsonrpc": "2.0",
  "id": "2",
  "method": "resources/read",
  "params": {
    "uri": "file:///project/readme.md"
  }
}
```

**도구 호출:**

```json
{
  "jsonrpc": "2.0",
  "id": "3",
  "method": "tools/call",
  "params": {
    "name": "search_files",
    "arguments": {
      "pattern": "*.ts",
      "directory": "/src"
    }
  }
}
```

---

### 2. Responses (응답)

**정의:** 응답은 요청에 대한 답변으로 전송되며, 작업의 결과 또는 오류를 포함합니다.

응답에는 두 가지 유형이 있습니다:

1. **Result Response (결과 응답)**: 성공 시
2. **Error Response (오류 응답)**: 실패 시

---

#### A. Result Responses (결과 응답)

**정의:** [결과 응답](https://claude.ai/specification/2025-11-25/schema#jsonrpcresultresponse)은 작업이 성공적으로 완료될 때 전송됩니다.

**메시지 구조:**

```json
{
  "jsonrpc": "2.0",
  "id": "req-123",
  "result": {
    "tools": [
      {
        "name": "search_files",
        "description": "Search for files",
        "inputSchema": { ... }
      }
    ]
  }
}
```

**필드 설명:**

|필드|타입|필수|설명|
|---|---|---|---|
|`jsonrpc`|string|✅|항상 "2.0"|
|`id`|string \| number|✅|원래 요청의 ID|
|`result`|object|✅|작업 결과|

---

**요구사항:**

- ✅ 결과 응답은 **반드시(MUST)** 대응하는 요청과 동일한 ID를 포함해야 함
- ✅ 결과 응답은 **반드시(MUST)** `result` 필드를 포함해야 함
- ✅ `result`는 **가능(MAY)** 한 모든 JSON 객체 구조를 따를 수 있음

**예시:**

**도구 목록 결과:**

```json
{
  "jsonrpc": "2.0",
  "id": "1",
  "result": {
    "tools": [
      {
        "name": "get_weather",
        "description": "Get weather information",
        "inputSchema": {
          "type": "object",
          "properties": {
            "location": { "type": "string" }
          }
        }
      }
    ]
  }
}
```

**리소스 읽기 결과:**

```json
{
  "jsonrpc": "2.0",
  "id": "2",
  "result": {
    "contents": [
      {
        "uri": "file:///project/readme.md",
        "mimeType": "text/markdown",
        "text": "# Project README\n..."
      }
    ]
  }
}
```

---

#### B. Error Responses (오류 응답)

**정의:** [오류 응답](https://claude.ai/specification/2025-11-25/schema#jsonrpcerrorresponse)은 작업이 실패하거나 오류가 발생할 때 전송됩니다.

**메시지 구조:**

```json
{
  "jsonrpc": "2.0",
  "id": "req-123",
  "error": {
    "code": -32602,
    "message": "Invalid params",
    "data": {
      "field": "location",
      "reason": "Required field missing"
    }
  }
}
```

**필드 설명:**

|필드|타입|필수|설명|
|---|---|---|---|
|`jsonrpc`|string|✅|항상 "2.0"|
|`id`|string \| number|⚠️|원래 요청의 ID (가능한 경우)|
|`error`|object|✅|오류 정보|
|`error.code`|number|✅|오류 코드 (정수)|
|`error.message`|string|✅|오류 메시지|
|`error.data`|any|❌|추가 오류 정보|

---

**요구사항:**

- ✅ 오류 응답은 **반드시(MUST)** 대응하는 요청과 동일한 ID를 포함해야 함
    - ⚠️ 예외: 잘못된 요청으로 인해 ID를 읽을 수 없는 경우
- ✅ 오류 응답은 **반드시(MUST)** `code` 및 `message`가 있는 `error` 필드를 포함해야 함
- ✅ 오류 코드는 **반드시(MUST)** 정수여야 함

---

**표준 JSON-RPC 오류 코드:**

|코드|의미|설명|
|---|---|---|
|-32700|Parse error|잘못된 JSON|
|-32600|Invalid Request|유효하지 않은 요청 객체|
|-32601|Method not found|메서드가 존재하지 않음|
|-32602|Invalid params|잘못된 메서드 매개변수|
|-32603|Internal error|내부 서버 오류|

**MCP 특정 오류 코드 범위:**

- `-32000` ~ `-32099`: 서버 정의 오류

---

**오류 응답 예시:**

**메서드를 찾을 수 없음:**

```json
{
  "jsonrpc": "2.0",
  "id": "1",
  "error": {
    "code": -32601,
    "message": "Method not found",
    "data": {
      "method": "tools/unknown"
    }
  }
}
```

**잘못된 매개변수:**

```json
{
  "jsonrpc": "2.0",
  "id": "2",
  "error": {
    "code": -32602,
    "message": "Invalid params",
    "data": {
      "field": "uri",
      "reason": "URI must be absolute"
    }
  }
}
```

**내부 오류:**

```json
{
  "jsonrpc": "2.0",
  "id": "3",
  "error": {
    "code": -32603,
    "message": "Internal error",
    "data": {
      "details": "Database connection failed"
    }
  }
}
```

---

### 3. Notifications (알림)

**정의:** [알림](https://claude.ai/specification/2025-11-25/schema#jsonrpcnotification)은 클라이언트에서 서버로 또는 그 반대로 단방향 메시지로 전송됩니다.

**메시지 구조:**

```json
{
  "jsonrpc": "2.0",
  "method": "notifications/resources/updated",
  "params": {
    "uri": "file:///project/data.json"
  }
}
```

**필드 설명:**

|필드|타입|필수|설명|
|---|---|---|---|
|`jsonrpc`|string|✅|항상 "2.0"|
|`method`|string|✅|알림 메서드 이름|
|`params`|object|❌|알림 매개변수|

---

**중요 규칙:**

- ❌ 알림은 **절대(MUST NOT)** ID를 포함해서는 안 됨
- ❌ 수신자는 **절대(MUST NOT)** 응답을 보내서는 안 됨

**특징:**

- 단방향 통신
- 응답 불필요
- Fire-and-forget 방식

---

**알림 예시:**

**리소스 업데이트 알림:**

```json
{
  "jsonrpc": "2.0",
  "method": "notifications/resources/updated",
  "params": {
    "uri": "file:///project/config.json"
  }
}
```

**진행 상황 알림:**

```json
{
  "jsonrpc": "2.0",
  "method": "notifications/progress",
  "params": {
    "progressToken": "task-123",
    "progress": 75,
    "total": 100
  }
}
```

**로그 메시지 알림:**

```json
{
  "jsonrpc": "2.0",
  "method": "notifications/message",
  "params": {
    "level": "info",
    "logger": "filesystem-server",
    "data": "File processed successfully"
  }
}
```

---

## 인증 (Auth)

MCP는 HTTP와 함께 사용하기 위한 [인증(Authorization)](https://claude.ai/specification/2025-11-25/basic/authorization) 프레임워크를 제공합니다.

### HTTP 기반 전송

**권장사항:**

- ✅ HTTP 기반 전송을 사용하는 구현은 **권장(SHOULD)** 이 사양을 준수해야 함

**인증 메커니즘:**

- OAuth 2.1 지원
- 토큰 기반 인증
- 세션 관리

**예시:**

```http
GET /mcp HTTP/1.1
Host: api.example.com
Authorization: Bearer eyJhbGciOiJSUzI1NiIs...
```

---

### STDIO 전송

**권장사항:**

- ❌ STDIO 전송을 사용하는 구현은 **권장하지 않음(SHOULD NOT)** 이 사양을 따르지 않아야 함

**대안:**

- 환경에서 자격 증명 검색
- 환경 변수 사용
- 구성 파일에서 읽기

**예시:**

```bash
# 환경 변수로 자격 증명 전달
API_KEY=your-api-key \
DATABASE_URL=postgresql://... \
node server.js
```

---

### 사용자 정의 인증

**옵션:**

- ✅ 클라이언트와 서버는 **가능(MAY)** 자체 사용자 정의 인증 및 권한 부여 전략을 협상할 수 있음

**예시:**

- 커스텀 헤더
- 서명 기반 인증
- 상호 TLS (mTLS)

---

### 커뮤니티 참여

**기여:** MCP의 인증 메커니즘 발전에 대한 추가 논의 및 기여는 다음을 참조하세요:

- [GitHub Discussions](https://github.com/modelcontextprotocol/specification/discussions)

---

## 스키마 (Schema)

### TypeScript 스키마 (진실의 원천)

**정의:** 프로토콜의 전체 사양은 [TypeScript 스키마](https://github.com/modelcontextprotocol/specification/blob/main/schema/2025-11-25/schema.ts)로 정의됩니다.

**역할:**

- 모든 프로토콜 메시지 및 구조의 진실의 원천
- 공식 참조 구현

**위치:**

```
https://github.com/modelcontextprotocol/specification/blob/main/schema/2025-11-25/schema.ts
```

---

### JSON 스키마 (자동 생성)

**정의:** [JSON 스키마](https://github.com/modelcontextprotocol/specification/blob/main/schema/2025-11-25/schema.json)는 TypeScript 진실의 원천에서 자동으로 생성됩니다.

**용도:**

- 다양한 자동화 도구와 함께 사용
- 검증 및 코드 생성
- IDE 자동 완성

**위치:**

```
https://github.com/modelcontextprotocol/specification/blob/main/schema/2025-11-25/schema.json
```

---

## JSON Schema 사용

Model Context Protocol은 프로토콜 전체에서 검증을 위해 JSON Schema를 사용합니다.

### Schema Dialect (스키마 방언)

MCP는 다음 규칙으로 JSON Schema를 지원합니다:

#### 1. 기본 방언 (Default Dialect)

**규칙:**

- 스키마에 `$schema` 필드가 포함되어 있지 않으면 JSON Schema 2020-12로 기본 설정됨

**기본값:**

```
https://json-schema.org/draft/2020-12/schema
```

**예시:**

```json
{
  "type": "object",
  "properties": {
    "name": { "type": "string" },
    "age": { 
      "type": "integer", 
      "minimum": 0 
    }
  },
  "required": ["name"]
}
```

위 스키마는 자동으로 JSON Schema 2020-12로 해석됩니다.

---

#### 2. 명시적 방언 (Explicit Dialect)

**규칙:**

- 스키마는 **가능(MAY)** `$schema` 필드를 포함하여 다른 방언을 지정할 수 있음

**예시 (draft-07):**

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "name": { "type": "string" },
    "age": { 
      "type": "integer", 
      "minimum": 0 
    }
  },
  "required": ["name"]
}
```

---

#### 3. 지원되는 방언

**필수:**

- ✅ 구현은 **반드시(MUST)** 최소한 JSON Schema 2020-12를 지원해야 함

**권장:**

- ✅ 구현은 **권장(SHOULD)** 추가로 지원하는 방언을 문서화해야 함

**일반적인 방언:**

- JSON Schema 2020-12 (권장)
- JSON Schema draft-07
- JSON Schema draft-04

---

#### 4. 권장사항

**모범 사례:**

- ✅ 구현자는 JSON Schema 2020-12를 사용하는 것이 **권장(RECOMMENDED)** 됨

**이유:**

- 최신 기능
- 더 나은 검증
- 광범위한 도구 지원

---

### 구현 요구사항

#### 필수 사항 (MUST)

**1. 기본 방언 지원:**

- ✅ 클라이언트와 서버는 **반드시(MUST)** 명시적 `$schema` 필드가 없는 스키마에 대해 JSON Schema 2020-12를 지원해야 함

**2. 방언별 검증:**

- ✅ 클라이언트와 서버는 **반드시(MUST)** 선언되거나 기본 방언에 따라 스키마를 검증해야 함

**3. 우아한 오류 처리:**

- ✅ 지원되지 않는 방언을 **반드시(MUST)** 우아하게 처리해야 함
- ✅ 방언이 지원되지 않음을 나타내는 적절한 오류 반환

**예시:**

```json
{
  "error": {
    "code": -32600,
    "message": "Unsupported schema dialect",
    "data": {
      "dialect": "http://json-schema.org/draft-03/schema#",
      "supported": [
        "https://json-schema.org/draft/2020-12/schema",
        "http://json-schema.org/draft-07/schema#"
      ]
    }
  }
}
```

---

#### 권장사항 (SHOULD)

**문서화:**

- ✅ 클라이언트와 서버는 **권장(SHOULD)** 지원하는 스키마 방언을 문서화해야 함

**문서 예시:**

```markdown
## 지원되는 JSON Schema 방언

이 구현은 다음 JSON Schema 방언을 지원합니다:

- JSON Schema 2020-12 (기본)
- JSON Schema draft-07
- JSON Schema draft-04

명시적 `$schema` 필드가 없는 스키마는 JSON Schema 2020-12로 
해석됩니다.
```

---

### 스키마 검증

**요구사항:**

- ✅ 스키마는 **반드시(MUST)** 선언되거나 기본 방언에 따라 유효해야 함

**검증 프로세스:**

```
1. 스키마 수신
    ↓
2. $schema 필드 확인
    ↓
3. 방언 결정 (명시적 또는 기본)
    ↓
4. 방언 지원 확인
    ↓
5. 스키마 검증
    ↓
6. 검증 성공 → 사용
   검증 실패 → 오류 반환
```

**검증 예시:**

```typescript
function validateSchema(schema: any): boolean {
  const dialect = schema.$schema || DEFAULT_DIALECT;
  
  if (!isSupportedDialect(dialect)) {
    throw new Error(`Unsupported dialect: ${dialect}`);
  }
  
  return validateAgainstDialect(schema, dialect);
}
```

---

## 일반 필드 (General Fields)

### 1. `_meta` 필드

**정의:** `_meta` 속성/매개변수는 MCP가 클라이언트와 서버가 상호작용에 추가 메타데이터를 첨부할 수 있도록 예약되어 있습니다.

---

#### 예약된 키

**프로토콜 수준 메타데이터:**

- 특정 키 이름은 MCP가 프로토콜 수준 메타데이터를 위해 예약
- 구현은 **절대(MUST NOT)** 이러한 키의 값에 대해 가정해서는 안 됨

**목적별 메타데이터:**

- [스키마](https://github.com/modelcontextprotocol/specification/blob/main/schema/2025-11-25/schema.ts)의 정의는 특정 이름을 목적별 메타데이터로 예약할 수 있음

---

#### 키 이름 형식

**구조:** 유효한 `_meta` 키 이름은 두 부분으로 구성됩니다:

1. **Prefix (접두사)**: 선택 사항
2. **Name (이름)**: 필수

---

#### Prefix (접두사) 규칙

**형식:**

```
prefix: label.label.label/
```

**규칙:**

**지정된 경우:**

- ✅ **반드시(MUST)** 점(`.`)으로 구분된 일련의 레이블 + 슬래시(`/`)로 끝나야 함

**레이블 규칙:**

- ✅ 레이블은 **반드시(MUST)** 문자로 시작해야 함
- ✅ 레이블은 **반드시(MUST)** 문자 또는 숫자로 끝나야 함
- ✅ 내부 문자는 문자, 숫자 또는 하이픈(`-`)일 수 있음

**권장사항:**

- ✅ 구현은 **권장(SHOULD)** 역방향 DNS 표기법을 사용해야 함
    - 예: `com.example/` (O)
    - 예: `example.com/` (X)

---

**예약된 접두사:**

**MCP 전용:** 두 번째 레이블이 `modelcontextprotocol` 또는 `mcp`인 모든 접두사는 MCP 용도로 **예약**됩니다.

**예약된 예시:**

```
io.modelcontextprotocol/     ✅ 예약됨
dev.mcp/                     ✅ 예약됨
org.modelcontextprotocol.api/ ✅ 예약됨
com.mcp.tools/               ✅ 예약됨
```

**예약되지 않은 예시:**

```
com.example.mcp/             ❌ 예약 안 됨 (두 번째 레이블이 'example')
org.company.tools/           ❌ 예약 안 됨
```

---

#### Name (이름) 규칙

**비어 있지 않은 경우:**

- ✅ **반드시(MUST)** 영숫자 문자(`[a-z0-9A-Z]`)로 시작하고 끝나야 함

**중간 문자:**

- ✅ **가능(MAY)** 하이픈(`-`), 언더스코어(`_`), 점(`.`), 영숫자 포함

**예시:**

```
✅ 올바른 이름:
- myKey
- my-key
- my_key
- my.key.name
- key123

❌ 잘못된 이름:
- -mykey  (하이픈으로 시작)
- mykey-  (하이픈으로 끝)
- my..key (연속 점)
```

---

#### `_meta` 사용 예시

**기본 메타데이터:**

```json
{
  "method": "tools/call",
  "params": {
    "name": "search",
    "_meta": {
      "requestId": "req-123",
      "timestamp": "2025-01-02T10:00:00Z"
    }
  }
}
```

**네임스페이스 메타데이터:**

```json
{
  "result": {
    "content": [...],
    "_meta": {
      "com.example/cacheHit": true,
      "com.example/latency": 45
    }
  }
}
```

**MCP 예약 메타데이터:**

```json
{
  "_meta": {
    "io.modelcontextprotocol/version": "2025-11-25",
    "dev.mcp/experimental": true
  }
}
```

---

### 2. `icons` 필드

**정의:** `icons` 속성은 서버가 리소스, 도구, 프롬프트 및 구현에 대한 시각적 식별자를 노출하는 표준화된 방법을 제공합니다.

**목적:**

- 시각적 컨텍스트 제공으로 사용자 인터페이스 향상
- 사용 가능한 기능의 검색 가능성 개선
- 일관된 브랜딩 및 식별

---

#### Icon 객체 구조

**필드:**

|필드|타입|필수|설명|
|---|---|---|---|
|`src`|string (URI)|✅|아이콘 리소스 URI|
|`mimeType`|string|❌|MIME 타입 (서버 타입이 누락되거나 일반적인 경우)|
|`sizes`|string[]|❌|크기 사양 배열|
|`theme`|string|❌|테마 선호도 (`light` 또는 `dark`)|

---

#### `src` URI 유형

**HTTP/HTTPS URL:**

```json
{
  "src": "https://example.com/icons/tool-icon.png"
}
```

**Data URI (Base64):**

```json
{
  "src": "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAUA..."
}
```

---

#### `sizes` 사양

**고정 크기:**

```json
{
  "sizes": ["48x48"]
}
```

**여러 크기:**

```json
{
  "sizes": ["48x48", "96x96", "192x192"]
}
```

**확장 가능 형식 (SVG):**

```json
{
  "sizes": ["any"]
}
```

---

#### `theme` 선호도

**밝은 배경용:**

```json
{
  "theme": "light"
}
```

**어두운 배경용:**

```json
{
  "theme": "dark"
}
```

---

#### 필수 MIME 타입 지원

**아이콘 렌더링을 지원하는 클라이언트는 반드시(MUST):**

**필수 지원:**

- ✅ `image/png` - PNG 이미지 (안전, 범용 호환성)
- ✅ `image/jpeg` (및 `image/jpg`) - JPEG 이미지 (안전, 범용 호환성)

**권장 지원:**

- ✅ `image/svg+xml` - SVG 이미지 (확장 가능하지만 보안 주의 필요)
- ✅ `image/webp` - WebP 이미지 (현대적이고 효율적인 형식)

---

#### 보안 고려사항

**중요:** 아이콘 메타데이터 소비자는 **반드시(MUST)** 손상을 방지하기 위해 적절한 보안 예방 조치를 취해야 합니다.

---

**1. 신뢰할 수 없는 입력으로 취급**

**규칙:**

- ✅ 아이콘 메타데이터와 아이콘 바이트를 신뢰할 수 없는 입력으로 취급
- ✅ 네트워크, 프라이버시, 구문 분석 위험에 대해 방어

---

**2. 안전한 URI 스키마**

**허용:**

- ✅ HTTPS URI
- ✅ `data:` URI

**거부:** 클라이언트는 **반드시(MUST)** 안전하지 않은 스키마 및 리디렉션을 거부해야 함:

- ❌ `javascript:`
- ❌ `file:`
- ❌ `ftp:`
- ❌ `ws:`
- ❌ 로컬 앱 URI 스키마

**추가 규칙:**

- ❌ 스키마 변경 불허
- ❌ 다른 출처의 호스트로 리디렉션 불허

---

**3. 리소스 소진 방지**

**방어:** 다음으로 인한 리소스 소진 공격에 대한 복원력:

- 과도한 크기의 이미지
- 큰 차원
- 과도한 프레임 (예: GIF)

**제한:**

- ✅ 소비자는 **가능(MAY)** 이미지 및 콘텐츠 크기 제한을 설정할 수 있음

**예시:**

```typescript
const MAX_IMAGE_SIZE = 5 * 1024 * 1024; // 5MB
const MAX_DIMENSIONS = 2048; // 2048x2048

if (imageSize > MAX_IMAGE_SIZE) {
  throw new Error("Image too large");
}

if (width > MAX_DIMENSIONS || height > MAX_DIMENSIONS) {
  throw new Error("Image dimensions too large");
}
```

---

**4. 자격 증명 없이 가져오기**

**규칙:**

- ❌ 쿠키 전송 금지
- ❌ `Authorization` 헤더 전송 금지
- ❌ 클라이언트 자격 증명 전송 금지

**이유:**

- 프라이버시 보호
- 자격 증명 유출 방지

---

**5. 동일 출처 확인**

**규칙:**

- ✅ 아이콘 URI가 서버와 동일한 출처에서 온 것인지 확인

**목적:**

- 타사에 데이터 노출 최소화
- 추적 정보 유출 방지

**검증 예시:**

```typescript
function verifyIconOrigin(iconUri: string, serverOrigin: string): boolean {
  const iconOrigin = new URL(iconUri).origin;
  return iconOrigin === serverOrigin;
}
```

---

**6. 실행 가능한 콘텐츠 주의**

**경고:** 페이로드에는 **가능(MAY)** 실행 가능한 콘텐츠가 포함될 수 있음:

- SVG의 [내장 JavaScript](https://www.w3.org/TR/SVG11/script.html)
- SVG의 [확장 기능](https://www.w3.org/TR/SVG11/extend.html)

**대응:**

- ✅ 소비자는 **가능(MAY)** 특정 파일 유형을 허용하지 않거나 렌더링 전에 아이콘 파일을 정리할 수 있음

**SVG 정리 예시:**

```typescript
function sanitizeSVG(svgContent: string): string {
  // JavaScript 제거
  svgContent = svgContent.replace(/<script\b[^<]*(?:(?!<\/script>)<[^<]*)*<\/script>/gi, '');
  
  // 이벤트 핸들러 제거
  svgContent = svgContent.replace(/\son\w+\s*=\s*"[^"]*"/gi, '');
  
  return svgContent;
}
```

---

**7. MIME 타입 및 파일 내용 검증**

**규칙:**

- ✅ 렌더링 전에 MIME 타입 및 파일 내용 검증
- ✅ MIME 타입 정보를 권고 사항으로 취급
- ✅ 매직 바이트를 통해 콘텐츠 타입 감지
- ✅ 불일치 또는 알 수 없는 타입 거부

**엄격한 허용 목록:**

- ✅ 이미지 타입의 엄격한 허용 목록 유지

**검증 예시:**

```typescript
const ALLOWED_MIME_TYPES = [
  'image/png',
  'image/jpeg',
  'image/jpg',
  'image/webp'
];

function validateImage(mimeType: string, content: Buffer): boolean {
  // MIME 타입 확인
  if (!ALLOWED_MIME_TYPES.includes(mimeType)) {
    return false;
  }
  
  // 매직 바이트로 실제 타입 확인
  const actualType = detectMimeType(content);
  
  // 불일치 거부
  if (actualType !== mimeType) {
    return false;
  }
  
  return true;
}
```

---

#### 사용처

**아이콘을 첨부할 수 있는 곳:**

**1. Implementation (구현):**

```json
{
  "protocolVersion": "2025-11-25",
  "capabilities": { ... },
  "serverInfo": {
    "name": "my-server",
    "version": "1.0.0",
    "icons": [
      {
        "src": "https://example.com/server-icon.png",
        "sizes": ["48x48"],
        "mimeType": "image/png"
      }
    ]
  }
}
```

**2. Tool (도구):**

```json
{
  "name": "search_files",
  "description": "Search for files",
  "icons": [
    {
      "src": "data:image/svg+xml;base64,PHN2Zy4uLg==",
      "sizes": ["any"],
      "mimeType": "image/svg+xml"
    }
  ],
  "inputSchema": { ... }
}
```

**3. Prompt (프롬프트):**

```json
{
  "name": "code_review",
  "description": "Review code",
  "icons": [
    {
      "src": "https://example.com/review-icon.png",
      "sizes": ["48x48", "96x96"],
      "theme": "light"
    }
  ],
  "arguments": [ ... ]
}
```

**4. Resource (리소스):**

```json
{
  "uri": "file:///project/readme.md",
  "name": "Project README",
  "icons": [
    {
      "src": "https://example.com/doc-icon.png",
      "sizes": ["48x48"],
      "mimeType": "image/png",
      "theme": "dark"
    }
  ],
  "mimeType": "text/markdown"
}
```

---

#### 여러 아이콘 제공

**목적:** 다양한 디스플레이 컨텍스트 및 해상도 지원

**예시:**

```json
{
  "icons": [
    {
      "src": "https://example.com/icon-48.png",
      "sizes": ["48x48"],
      "mimeType": "image/png"
    },
    {
      "src": "https://example.com/icon-96.png",
      "sizes": ["96x96"],
      "mimeType": "image/png"
    },
    {
      "src": "https://example.com/icon.svg",
      "sizes": ["any"],
      "mimeType": "image/svg+xml"
    }
  ]
}
```

**클라이언트 선택:** 클라이언트는 UI 요구사항에 따라 가장 적절한 아이콘을 선택해야 합니다.

---

## 요약

### 프로토콜 구성

**필수 구성 요소:**

- ✅ Base Protocol (JSON-RPC 2.0)
- ✅ Lifecycle Management

**선택적 구성 요소:**

- 📋 Authorization
- 📋 Server Features
- 📋 Client Features
- 📋 Utilities

### 메시지 유형

**Requests:**

- ID 필수 (null 불가)
- 메서드 및 매개변수

**Responses:**

- Result: 성공 시
- Error: 실패 시

**Notifications:**

- ID 없음
- 단방향 메시지

### JSON Schema

**기본 방언:**

- JSON Schema 2020-12

**지원:**

- 필수: 2020-12
- 권장: draft-07 등 문서화

### 일반 필드

**`_meta`:**

- 메타데이터 첨부
- 네임스페이스 지원
- MCP 예약 키

**`icons`:**

- 시각적 식별자
- 여러 크기 지원
- 엄격한 보안 요구사항

MCP 기본 프로토콜은 안전하고 확장 가능한 AI 통합을 위한 견고한 기반을 제공합니다!

---

## 다음 단계

### 학습 경로

1. **수명 주기**
    
    - [Lifecycle](https://claude.ai/specification/2025-11-25/basic/lifecycle)
    - 연결 초기화
    - 기능 협상
2. **전송**
    
    - [Transports](https://claude.ai/specification/2025-11-25/basic/transports)
    - STDIO vs HTTP
3. **인증**
    
    - [Authorization](https://claude.ai/specification/2025-11-25/basic/authorization)
    - OAuth 2.1

### 추가 리소스

- **아키텍처**: [Architecture](https://claude.ai/specification/2025-11-25/architecture/index)
- **스키마**: [Schema Reference](https://claude.ai/specification/2025-11-25/schema)
- **GitHub**: https://github.com/modelcontextprotocol

---

_이 문서는 Model Context Protocol 공식 기본 프로토콜 사양에서 가져온 내용입니다._