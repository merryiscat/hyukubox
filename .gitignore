# Hyukebox - Git Ignore Rules

# Python 관련
__pycache__/          # Python 바이트코드 캐시
*.py[cod]             # 컴파일된 Python 파일 (.pyc, .pyo, .pyd)
*$py.class            # Python 클래스 파일
*.so                  # 네이티브 확장 모듈
.Python               # Python 설정 파일

# 가상환경 (venv, virtualenv)
venv/
env/
ENV/
.venv/

# uv 관련
.uv/                  # uv 캐시 디렉토리
uv.lock              # uv 의존성 잠금 파일 (Git에 포함 가능하지만 선택 사항)

# 환경 변수 파일 (민감 정보 포함!)
.env                  # API 키 등이 포함된 환경 변수 파일
.env.local
.env.*.local

# 캐시 디렉토리 및 데이터베이스
cache/                # TinyDB 캐시 디렉토리
*.db                  # SQLite 데이터베이스 파일
*.json.bak            # TinyDB 백업 파일
*.sqlite
*.sqlite3

# IDE 및 에디터 설정
.vscode/              # Visual Studio Code 설정
.idea/                # PyCharm/IntelliJ IDEA 설정
*.swp                 # Vim swap 파일
*.swo                 # Vim swap 파일
*.swn                 # Vim swap 파일
.DS_Store             # macOS 파일 탐색기 설정

# 테스트 및 커버리지
.pytest_cache/        # pytest 캐시
.coverage             # coverage.py 데이터 파일
htmlcov/              # HTML 커버리지 리포트
.tox/                 # tox 테스트 환경
.nox/                 # nox 세션
coverage.xml          # 커버리지 XML 리포트
*.cover               # 커버리지 데이터 파일

# 빌드 및 배포
dist/                 # 배포용 빌드 파일
build/                # 빌드 임시 파일
*.egg-info/           # Python egg 메타데이터
.eggs/                # setuptools egg 파일

# 로그 파일
*.log                 # 모든 로그 파일
logs/                 # 로그 디렉토리

# OS 관련
Thumbs.db             # Windows 썸네일 캐시
*.stackdump           # Cygwin 스택 덤프

# 임시 파일
*.tmp
*.temp
*.bak
*.backup
~$*

# Jupyter Notebook
.ipynb_checkpoints/   # Jupyter 체크포인트

# mypy 타입 체커
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre 타입 체커
.pyre/

# pytype 정적 분석기
.pytype/

# Cython 디버그 심볼
cython_debug/
